import styled from 'styled-components'
import { Col } from '../Grid'

const TableContainer = styled(Col)``

const TableHead = styled(Col)``

const TableContent = styled(Col)``

export { TableContainer, TableHead, TableContent }
